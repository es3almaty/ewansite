(function(){
  function $(s){return document.querySelector(s);} function esc(v){return String(v||'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');}
  function modeSlug(mode){const m=String(mode||'').toLowerCase(); if(m.includes('human')) return 'human'; if(m.includes('automate')) return 'automate'; if(m.includes('advise')) return 'advise'; return 'assist';}
  function getData(){return (window.AIOS_FULL_EXAMPLES && window.AIOS_FULL_EXAMPLES.examples) || [];}
  function find(slug){return getData().find(e=>e.slug===slug);}
  function modeBars(modes){return `<div class="aios-mode-list">${modes.map(m=>`<div class="aios-bar-row aios-mode-${modeSlug(m.mode)}"><div class="aios-bar-label">${esc(m.mode)}</div><div class="aios-bar-track"><div class="aios-bar-fill" style="width:${Number(m.pct||0)}%"></div></div><div class="aios-small">${Number(m.pct||0).toFixed(1)}%</div></div>`).join('')}</div>`;}
  function clusterBars(items){return `<div class="aios-mode-list">${items.map(c=>`<div class="aios-bar-row"><div class="aios-bar-label">${esc(c.label)}</div><div class="aios-bar-track"><div class="aios-bar-fill" style="width:${Number(c.pct||0)}%"></div></div><div class="aios-small">${Number(c.baselineHours||0).toFixed(1)}h</div></div>`).join('')}</div>`;}
  function card(e){const d={}; (e.modes||[]).forEach(m=>d[modeSlug(m.mode)]=m.pct||0); return `<a class="aios-card aios-library-card" href="/ai-os/examples/${esc(e.slug)}.html"><div><div class="aios-band">${esc(e.impactBand)}</div><h3>${esc(e.title)}</h3><p class="aios-small">${esc(e.headline)}</p></div><div><strong>${esc(e.metrics.savingPct)}% task-time saving</strong><p class="aios-small">${esc(e.metrics.hoursSaved)} hours saved from a 120-hour monthly baseline.</p><div class="aios-mini-meter"><div style="width:${d.automate||0}%"></div><div style="width:${d.assist||0}%"></div><div style="width:${d.advise||0}%"></div><div style="width:${d.human||0}%"></div></div><p class="aios-small">Automate · Assist · Advise · Human-only</p></div></a>`;}
  function renderLibrary(){const root=$('#aios-library-root'); if(!root) return; root.innerHTML=`<div class="aios-library-grid">${getData().map(card).join('')}</div>`;}
  function renderEmbed(){const root=$('#aios-work-rebundler-examples'); if(!root) return; root.innerHTML=`<section class="aios-shell aios-section aios-embed"><h2>AI-OS Workbench examples</h2><p class="aios-section-intro">These five worked examples show the deeper Workbench layer behind the public role generator: task decomposition, AI mode assignment, residual effort, Decision Compass risk, control burden, and capacity compression.</p><div class="aios-library-grid">${getData().map(card).join('')}</div></section>`;}
  function renderExample(){const root=$('#aios-example-root'); if(!root) return; const e=find(root.getAttribute('data-example')); if(!e){root.innerHTML='<section class="aios-shell aios-section"><div class="aios-card"><h1>Example not found</h1></div></section>';return;} document.title=`AI-OS Example: ${e.title} · Ewan Simpson`; root.innerHTML=`
<header class="aios-hero"><div class="aios-shell aios-hero-grid"><div><div class="aios-eyebrow">AI-OS workbench example · ${esc(e.onetCode)}</div><h1 class="aios-title">${esc(e.title)}</h1><p class="aios-subtitle">${esc(e.executiveSummary)}</p><div class="aios-button-row"><a class="aios-button primary" href="${esc(e.pdfHref)}">Download PDF report</a><a class="aios-button" href="/ai-os/examples/">View all examples</a></div></div><aside class="aios-panel dark"><div class="aios-eyebrow">${esc(e.impactBand)}</div><p>${esc(e.publicMessage)}</p><div class="aios-kpi-grid"><div class="aios-kpi"><div class="aios-kpi-value">${esc(e.metrics.tasks)}</div><div class="aios-kpi-label">Tasks assessed</div></div><div class="aios-kpi"><div class="aios-kpi-value">${esc(e.metrics.savingPct)}%</div><div class="aios-kpi-label">Estimated task-time saving</div></div><div class="aios-kpi"><div class="aios-kpi-value">${esc(e.metrics.hoursSaved)}h</div><div class="aios-kpi-label">Monthly hours released</div></div><div class="aios-kpi"><div class="aios-kpi-value">${esc(e.controlBurden)}</div><div class="aios-kpi-label">Control burden</div></div></div></aside></div></header>
<main><section class="aios-shell aios-section"><h2>Method in brief</h2><p class="aios-section-intro">The model starts with O*NET task statements, normalizes each occupation to 120 productive monthly hours, allocates time across tasks using importance, relevance, frequency and task type, then applies AI-OS mode assumptions and Decision Compass risk controls.</p><div class="aios-grid-3"><div class="aios-card"><h3>Decompose</h3><p class="aios-small">The job title is unpacked into task-level work. Each row is a task, not a vague role label.</p></div><div class="aios-card"><h3>Classify</h3><p class="aios-small">Tasks are assigned to Automate, Assist, Advise, or Human-only, then reviewed against risk factors.</p></div><div class="aios-card"><h3>Rebundle</h3><p class="aios-small">The output is a redesign view: what human work remains, what controls are needed, and what capacity is released.</p></div></div></section>
<section class="aios-shell aios-section"><div class="aios-grid-2"><div class="aios-card"><h2>Time compression</h2><p class="aios-section-intro">Savings are task-time estimates, not headcount claims.</p><div class="aios-compression"><div class="metric"><strong>${esc(e.metrics.baselineMonthlyHours)}h</strong><p class="aios-small">Baseline monthly work</p></div><div class="metric"><strong>${esc(e.metrics.futureMonthlyHours)}h</strong><p class="aios-small">Future residual work</p></div><div class="metric"><strong>${esc(e.metrics.hoursSaved)}h</strong><p class="aios-small">Monthly capacity released</p></div></div></div><div class="aios-card"><h2>AI mode distribution</h2><p class="aios-section-intro">Distribution by task count. The compression estimate is weighted by monthly task-time.</p>${modeBars(e.modes)}</div></div></section>
<section class="aios-shell aios-section"><div class="aios-grid-2"><div class="aios-card"><h2>Task cluster profile</h2><p class="aios-section-intro">Baseline hours by task cluster.</p>${clusterBars(e.clusters)}</div><div class="aios-card"><h2>Risk and capability map</h2><div class="aios-risk-grid">${e.riskCapabilityCells.map(c=>`<div class="aios-risk-cell"><strong>${esc(c.title)}</strong><span>${esc(c.text)}</span></div>`).join('')}</div></div></div></section>
<section class="aios-shell aios-section"><h2>Priority task examples</h2><p class="aios-section-intro">Top tasks by estimated released hours, with AI mode, Decision Compass score, and control requirement.</p><div class="aios-task-list">${e.priorityTasks.map(t=>`<article class="aios-task"><div><h4>${esc(t.task)}</h4><p class="aios-small"><strong>Control:</strong> ${esc(t.control)}</p><p class="aios-small"><strong>Oversight:</strong> ${esc(t.oversight)}</p><div class="aios-task-meta"><span class="aios-pill ${modeSlug(t.mode)}">${esc(t.mode)}</span><span class="aios-pill">${esc(t.hoursSaved)}h saved</span></div></div><div class="aios-task-score"><div class="aios-score-number">${esc(t.decisionCompass)}</div><div class="aios-small">Decision Compass score</div></div></article>`).join('')}</div></section>
<section class="aios-shell aios-section"><div class="aios-grid-2"><div class="aios-card"><h2>Rebundled role direction</h2><ul class="aios-small">${e.rebundledRole.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div class="aios-card"><h2>Control architecture</h2><ul class="aios-small">${e.controlArchitecture.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></div></section></main><footer class="aios-shell aios-footer-note">© 2026 Ewan Simpson. AI-OS examples are illustrative task-layer assessments based on O*NET data and AI-OS assumptions. They are not legal, HR, employment, or financial advice.</footer>`;}
  document.addEventListener('DOMContentLoaded',function(){renderLibrary();renderEmbed();renderExample();});
})();
